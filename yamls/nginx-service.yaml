apiVersion: v1
kind: Service
metadata:
  labels:
    app: nginx-app2
  name: nginx-app2
  namespace: prod3
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: nginx-app2
  type: LoadBalancer
